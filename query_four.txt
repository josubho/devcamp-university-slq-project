USE university_schema;

SELECT AVG(g.grades_values) AS "Average Grade", c.course_subject AS "Subject"
FROM grades g
JOIN courses c
ON g.grades_course_id = c.course_id
GROUP BY g.grades_course_id
ORDER BY AVG(g.grades_values) ASC;