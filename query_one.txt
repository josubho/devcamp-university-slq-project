USE university_schema;

SELECT AVG(g.grades_values) as "avg", p.professor_name AS "name"
FROM grades g
JOIN professors p
ON g.grades_professor_id = p.professor_id
WHERE grades_values IS NOT NULL
GROUP BY grades_professor_id
ORDER BY avg DESC;